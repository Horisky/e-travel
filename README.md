# e-travel

This repo contains a minimal FastAPI backend to generate day-by-day travel itineraries in Chinese.

Structure:
- `backend/` FastAPI app
- `frontend/` Next.js app

Next step: connect your frontend and deploy (Vercel + any Python host).
